<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ApiConsumePeliculas" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/ColorPrimario</item>
        <item name="android:windowIsFloating">false</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ApiConsumePeliculas" parent="Base.Theme.ApiConsumePeliculas" />
</resources>